# 货车分析API文档

本文档详细说明了10个货车分析相关的复杂API接口，这些接口专注于货车（车型11-16）的通行数据分析。

## API概览

| 序号 | API名称 | 接口路径 | 分类 |
|-----|--------|---------|------|
| 1 | 路段货车小时流量 | `/api/analytics/truck/hourly-flow` | 📊 流量统计类 |
| 2 | 路段平均通行时间 | `/api/analytics/truck/avg-travel-time` | ⏱️ 通行时效类 |
| 3 | 路段平均通行费 | `/api/analytics/truck/avg-toll-fee` | 💰 费用分析类 |
| 4 | 路段拥堵指数 | `/api/analytics/truck/congestion-index` | 📊 流量统计类 |
| 5 | 路段超载货车比例 | `/api/analytics/truck/overweight-rate` | ⚖️ 合规监测类 |
| 6 | 路段通行费优惠比例 | `/api/analytics/truck/discount-rate` | 💰 费用分析类 |
| 7 | 路段货车高峰时段 | `/api/analytics/truck/peak-hours` | 📊 流量统计类 |
| 8 | 路段货车平均轴数 | `/api/analytics/truck/avg-axle-count` | ⚖️ 合规监测类 |
| 9 | 路段车道利用率 | `/api/analytics/truck/lane-utilization` | 📊 流量统计类 |
| 10 | 路段收费站运行状态 | `/api/analytics/truck/toll-station-status` | 📈 基础指标类 |

---

## 详细说明

### 1. 路段货车小时流量 📊

**接口**: `GET /api/analytics/truck/hourly-flow`

**描述**: 统计每个路段每小时通过的货车数量，反映路段流量强度。

**业务价值**: 用于识别高流量路段，辅助避开拥堵、合理分配路径。

**请求参数**:
- `section_id` (可选): 路段ID，不提供则查询所有路段
- `start_date` (可选): 开始时间，格式：`YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS`
- `end_date` (可选): 结束时间，格式：`YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS`

**示例请求**:
```bash
GET /api/analytics/truck/hourly-flow?section_id=G5615530120&start_date=2023-01-03
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "hour": "2023-01-03 14",
      "truck_count": 25
    }
  ],
  "count": 24,
  "category": "📊 流量统计类",
  "description": "统计每个路段每小时通过的货车数量"
}
```

---

### 2. 路段平均通行时间 ⏱️

**接口**: `GET /api/analytics/truck/avg-travel-time`

**描述**: 统计每个路段货车的平均通行时间（出口时间-入口时间）。

**业务价值**: 为路径规划提供时效预估，辅助选择更快路线。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/avg-travel-time?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "avg_travel_time_minutes": 45.32,
      "sample_count": 1523
    }
  ],
  "count": 1,
  "category": "⏱️ 通行时效类",
  "description": "统计每个路段货车的平均通行时间（分钟）"
}
```

---

### 3. 路段平均通行费 💰

**接口**: `GET /api/analytics/truck/avg-toll-fee`

**描述**: 统计每个路段货车的平均通行费用（元）。

**业务价值**: 为路径规划提供成本参考，支持低成本路线选择。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/avg-toll-fee?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "avg_toll_fee": 35.67,
      "avg_toll_money": 35.67,
      "transaction_count": 2340
    }
  ],
  "count": 1,
  "category": "💰 费用分析类",
  "description": "统计每个路段货车的平均通行费用（元）"
}
```

---

### 4. 路段拥堵指数 📊

**接口**: `GET /api/analytics/truck/congestion-index`

**描述**: 通过货车流量与车道数比值，评估路段拥堵程度。

**业务价值**: 辅助路径规划避开高拥堵路段，提高通行效率。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/congestion-index?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "truck_count": 5432,
      "avg_lane_count": 4.5,
      "congestion_index": 1207.11
    }
  ],
  "count": 1,
  "category": "📊 流量统计类",
  "description": "通过货车流量与车道数比值评估路段拥堵程度"
}
```

**说明**: `congestion_index` 数值越大表示拥堵程度越高。

---

### 5. 路段超载货车比例 ⚖️

**接口**: `GET /api/analytics/truck/overweight-rate`

**描述**: 统计每个路段超载货车（总重量>限制）的比例，反映合规风险。

**业务价值**: 辅助规避高风险路段，降低合规处罚概率。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/overweight-rate?section_id=G5615530120
```

**数据脱敏**: 本接口对 `section_id` 字段进行掩码脱敏处理，保护路段隐私。

**响应示例（处理前 vs 处理后）**:

处理前（原始数据）:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "total_count": 2340,
      "overweight_count": 356,
      "overweight_rate": 0.1521,
      "overweight_percentage": 15.21
    }
  ],
  "category": "⚖️ 合规监测类"
}
```

处理后（脱敏数据）:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G*********0",
      "section_id_masked": true,
      "total_count": 2340,
      "overweight_count": 356,
      "overweight_rate": 0.1521,
      "overweight_percentage": 15.21
    }
  ],
  "category": "⚖️ 合规监测类",
  "data_masking": {
    "enabled": true,
    "method": "掩码(Masking)",
    "fields": ["section_id"],
    "description": "对路段ID进行掩码处理，保留首尾字符，中间用*替换"
  }
}
```

---

### 6. 路段通行费优惠比例 💰

**接口**: `GET /api/analytics/truck/discount-rate`

**描述**: 统计每个路段享受通行费优惠的货车比例。

**业务价值**: 辅助货车选择优惠路段，优化通行成本。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/discount-rate?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "discount_rate": 0.3456,
      "discount_percentage": 34.56,
      "total_count": 2340,
      "discount_count": 809
    }
  ],
  "count": 1,
  "category": "💰 费用分析类",
  "description": "统计每个路段享受通行费优惠的货车比例"
}
```

---

### 7. 路段货车高峰时段 📊

**接口**: `GET /api/analytics/truck/peak-hours`

**描述**: 识别每个路段货车流量最高的小时区间。

**业务价值**: 辅助避开高峰时段，提升通行效率。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/peak-hours?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "hour": 14,
      "truck_count": 234
    }
  ],
  "count": 1,
  "category": "📊 流量统计类",
  "description": "识别每个路段货车流量最高的小时区间",
  "all_hourly_data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "hour": 14,
      "truck_count": 234
    },
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "hour": 15,
      "truck_count": 198
    }
  ]
}
```

**说明**: `data` 返回每个路段的高峰时段，`all_hourly_data` 返回所有小时的详细数据。

---

### 8. 路段货车平均轴数 ⚖️

**接口**: `GET /api/analytics/truck/avg-axle-count`

**描述**: 统计每个路段货车的平均轴数，反映货车类型分布。

**业务价值**: 辅助理解货车流量结构，支持车型相关路径优化。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/avg-axle-count?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "avg_axle_count": 4.23,
      "sample_count": 2340
    }
  ],
  "count": 1,
  "category": "⚖️ 合规监测类",
  "description": "统计每个路段货车的平均轴数，反映货车类型分布"
}
```

---

### 9. 路段车道利用率 📊

**接口**: `GET /api/analytics/truck/lane-utilization`

**描述**: 统计每个路段货车流量与车道数的比值，反映车道资源利用情况。

**业务价值**: 辅助评估路段通行能力，优化路径分配。

**请求参数**:
- `section_id` (可选): 路段ID
- `start_date` (可选): 开始时间
- `end_date` (可选): 结束时间

**示例请求**:
```bash
GET /api/analytics/truck/lane-utilization?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "section_name": "河段",
      "total_transactions": 5432,
      "total_lanes": 45.0,
      "lane_utilization": 120.71
    }
  ],
  "count": 1,
  "category": "📊 流量统计类",
  "description": "统计每个路段货车流量与车道数的比值，反映车道资源利用情况"
}
```

---

### 10. 路段收费站运行状态 📈

**接口**: `GET /api/analytics/truck/toll-station-status`

**描述**: 统计每个路段收费站的运行状态（正常/关闭/维护等）。

**业务价值**: 辅助路径规划避开关闭或异常收费站，保障通行顺畅。

**请求参数**:
- `section_id` (可选): 路段ID

**示例请求**:
```bash
GET /api/analytics/truck/toll-station-status?section_id=G5615530120
```

**响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "section_id": "G5615530120",
      "toll_station_id": "G5615530120010",
      "station_name": "新宝站",
      "station_type": "1",
      "operation_status": "1",
      "station_status": "1"
    }
  ],
  "count": 10,
  "status_summary": {
    "1": 8,
    "2": 2
  },
  "category": "📈 基础指标类",
  "description": "统计每个路段收费站的运行状态（正常/关闭/维护等）"
}
```

---

## 通用说明

### 货车车型定义
所有API中的货车定义为车型类别 11-16：
- `11`: 货车类型1
- `12`: 货车类型2
- `13`: 货车类型3
- `14`: 货车类型4
- `15`: 货车类型5
- `16`: 货车类型6

### 日期时间格式
- 支持格式：`YYYY-MM-DD` 或 `YYYY-MM-DD HH:MM:SS`
- 示例：`2023-01-03` 或 `2023-01-03 14:30:00`

### 错误响应
所有API在发生错误时返回统一格式：
```json
{
  "success": false,
  "error": "错误描述信息"
}
```

### 性能建议
1. 查询大量数据时，建议添加时间范围限制
2. 优先使用 `section_id` 参数缩小查询范围
3. 对于实时监控场景，建议设置合理的查询间隔

---

## 使用场景示例

### 场景1：路径规划优化
组合使用以下API为货车规划最优路径：
1. `/api/analytics/truck/avg-travel-time` - 获取最快路线
2. `/api/analytics/truck/avg-toll-fee` - 获取最便宜路线
3. `/api/analytics/truck/congestion-index` - 避开拥堵路段
4. `/api/analytics/truck/peak-hours` - 避开高峰时段

### 场景2：合规风险评估
使用以下API评估路段合规风险：
1. `/api/analytics/truck/overweight-rate` - 超载风险评估
2. `/api/analytics/truck/avg-axle-count` - 车型匹配度分析
3. `/api/analytics/truck/toll-station-status` - 收费站状态监控

### 场景3：成本优化分析
使用以下API优化通行成本：
1. `/api/analytics/truck/avg-toll-fee` - 路段成本对比
2. `/api/analytics/truck/discount-rate` - 优惠路段识别
3. `/api/analytics/truck/peak-hours` - 避峰降费策略

---

## 测试工具

运行测试脚本：
```bash
python test_truck_analytics.py
```

测试脚本会自动测试所有10个API接口并输出结果。
